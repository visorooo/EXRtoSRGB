[pytest]
testpaths = tests
addopts = -q

# No cache_dir override here: pytest does not expand environment variables in
# this file, so "%(TEMP)s/..." creates a directory named literally that. The
# default .pytest_cache is dot-hidden and gitignored, which is good enough.
